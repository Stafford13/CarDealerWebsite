using System;

namespace RPS._2
{
    class Program
    {
        static void Main(string[] args)
        {
            //greet user

            // start loop1
            // (METHOD) ask user how many rounds they'd like to play (x)
            // // for (int rounds = 0; rounds >= 1 && rounds <= 10; rounds++)
            // if (rounds < 1 || rounds > 10)
            // Say "That isn't a correct number, ya dingus!"
            // else
            // playGame(rounds)

            // start loop2 each round
            // for (int q = 1; q <= #rounds; q++)
            // if (q <= #rounds)
            // user chooses r, p, or s
            // computer chooses r, p, or s
            // determine if win, tie or loss
            // keep track of win, tie, and loss
            // if (q > # rounnds)
            // break;
            // end loop 2 after x rounds

            // after #rounds - display number of wins, losses, and ties

            // start loop3
            // if 
            // wins are greater than losses - victory
            // else if 
            // losses are greater than wins - defeat
            // else 
            // uhhh idk
            // end loop3

            // would the user like to play again
            //y = start over at "METHOD how many rounds'
            //n = thanks for playing! Quit.
            // emd loop1
        }
    }
}
